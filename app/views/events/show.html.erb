<div class="titleSection">
  <%= image_tag @event.currentPhoto(:thumb) %>
  <div class="title">
    <h2 class="HV"><%= @event.title %></h2>
    <div class="date">
      <%= @event.start_date.strftime("%Y.%m.%d")%>-<%= @event.end_date.strftime("%Y.%m.%d")%>
    </div>
  </div>
</div>
<div class="CB"></div>
<div class="descriptionSection">
  <div class="description">
    <div style="margin-right:10px">
      <%= raw(@event.description) %>
    </div>
    <%= link_to 'Back', exhibitions_path, :class => 'backBtn' %>
    <%= link_to 'Download PDF', '#', :class => 'downloadBtn' %>
	<div class="CB"></div>
  </div>

  <div class="photo">
    <%= image_tag @event.bigphoto.url(:original) %>
  </div>
</div>